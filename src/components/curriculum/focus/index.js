import React from 'react';

// components
import Wrapper from '../../wrapper';
import FocusWrap from './FocusContent';

const FocusContent = () => {
    return <Wrapper>
        <FocusWrap />
    </Wrapper>;
};

export default FocusContent;